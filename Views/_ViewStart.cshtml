@* _ViewStart.cshtml - Runs before every view is rendered
   Sets the default layout file to use *@

@{
    // This tells every view to use the _Layout.cshtml as its wrapper/template
    // You can override this in individual views if needed
    Layout = "_Layout";
}
